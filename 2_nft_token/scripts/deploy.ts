import { ethers } from "hardhat";

async function main() {
  // Deployment code needs to be added

  // find more information on following link
  // https://hardhat.org/hardhat-runner/docs/guides/deploying
}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});
